import type { ModuleContext } from '../module-context'

// STUB — được thay thế bởi agent module 'downloader' (spec 4.10)
export default function register(_ctx: ModuleContext): void {
  // chưa có channel
}
